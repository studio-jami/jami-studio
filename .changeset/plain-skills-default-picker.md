---
"@agent-native/skills": patch
---

Keep the default add/list flows and public-repo-backed app skills on the live BuilderIO skills collection instead of delegating them to the app-backed core picker.

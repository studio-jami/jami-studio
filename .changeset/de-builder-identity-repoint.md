---
"@agent-native/core": patch
---

Repoint self-identity to `studio-jami/jami-studio`: CLI `REPO` template source, bug-report URL, error-report issue prefill, Open Source badge link, onboarding OSS link, and generated plugin-manifest `repository` field. Live upstream integrations (skills catalog, recap reusable workflow, release downloads) intentionally unchanged.
